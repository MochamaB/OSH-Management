@model OSHManagement.Models.ViewModels.ChangePasswordViewModel
@{
    ViewData["Title"] = "Change Password";
    Layout = "_AuthenticationLayout";
}

<div class="text-center mb-4">
    <img src="~/assets/images/brand-logos/logo.gif" alt="KTDA Logo" class="mb-3" style="height: 60px;">
    <h4 class="mb-1 fw-semibold">Change Password</h4>
    <p class="mb-4 text-muted fw-normal">Update your account password</p>
</div>

<form asp-action="ChangePassword" method="post" class="row gy-3">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    
    <div class="col-xl-12">
        <label asp-for="CurrentPassword" class="form-label text-default"></label>
        <div class="input-group">
            <input asp-for="CurrentPassword" class="form-control form-control-lg" placeholder="Enter current password" id="current-password-input">
            <button class="btn btn-light" type="button" onclick="createpassword('current-password-input','current-password-addon')" id="current-password-addon">
                <i class="ri-eye-off-line align-middle"></i>
            </button>
        </div>
        <span asp-validation-for="CurrentPassword" class="text-danger"></span>
    </div>
    
    <div class="col-xl-12">
        <label asp-for="NewPassword" class="form-label text-default"></label>
        <div class="input-group">
            <input asp-for="NewPassword" class="form-control form-control-lg" placeholder="Enter new password" id="new-password-input">
            <button class="btn btn-light" type="button" onclick="createpassword('new-password-input','new-password-addon')" id="new-password-addon">
                <i class="ri-eye-off-line align-middle"></i>
            </button>
        </div>
        <span asp-validation-for="NewPassword" class="text-danger"></span>
    </div>
    
    <div class="col-xl-12">
        <label asp-for="ConfirmPassword" class="form-label text-default"></label>
        <div class="input-group">
            <input asp-for="ConfirmPassword" class="form-control form-control-lg" placeholder="Confirm new password" id="confirm-password-input">
            <button class="btn btn-light" type="button" onclick="createpassword('confirm-password-input','confirm-password-addon')" id="confirm-password-addon">
                <i class="ri-eye-off-line align-middle"></i>
            </button>
        </div>
        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
    </div>
    
    <div class="col-xl-12 d-grid mt-4">
        <button type="submit" class="btn btn-lg btn-primary">Update Password</button>
    </div>
</form>

<div class="text-center mt-4">
    <p class="fs-12 text-muted mb-0">
        <a class="text-primary" asp-controller="Dashboard" asp-action="Index">Back to Dashboard</a>
    </p>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function createpassword(inputId, buttonId) {
            var passwordInput = document.getElementById(inputId);
            var passwordButton = document.getElementById(buttonId);
            var icon = passwordButton.querySelector('i');
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                icon.className = 'ri-eye-line align-middle';
            } else {
                passwordInput.type = 'password';
                icon.className = 'ri-eye-off-line align-middle';
            }
        }
    </script>
}
